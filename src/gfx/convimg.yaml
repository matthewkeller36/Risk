palettes:
  - name: global_palette
    images: automatic

converts:
  - name: sprites
    palette: global_palette
    images:
      - RiskMap.png

outputs:
  - type: c
    include-file: gfx.h
    palettes:
      - global_palette
    converts:
      - sprites
